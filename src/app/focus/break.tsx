import { RoutePlaceholder } from '@/components/route-placeholder';

export default function TrueZenBreakRoute() {
  return <RoutePlaceholder title="True Zen Break" description="The optional recovery timer and supported recovery guidance will be implemented later." actions={[{ label: 'Continue to Session Summary', href: '/focus/summary' }]} />;
}
