import { RoutePlaceholder } from '@/components/route-placeholder';

export default function RewardsRoute() {
  return <RoutePlaceholder title="Rewards" description="Verified streak, XP, level, and achievement progress will appear here." />;
}
